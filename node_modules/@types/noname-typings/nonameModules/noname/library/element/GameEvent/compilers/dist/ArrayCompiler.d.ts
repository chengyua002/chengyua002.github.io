export default class ArrayCompiler extends ContentCompilerBase {
    type: string;
    filter(content: any): content is any[];
    compile(content: any): (event: any) => Promise<void>;
}
import ContentCompilerBase from "./ContentCompilerBase.js";
